export interface Propiedad{
    propiedad_id: number,
    nombre: string,
    direccion: string,
    distrito: string,
    valor_vivienda: number,
    moneda_id: number,

    bbp: boolean,
    periodo_gracia: boolean
}